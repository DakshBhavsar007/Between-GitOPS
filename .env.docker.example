# Local Docker Compose Environment Configuration
DATABASE_URL=postgresql://postgres:postgrespassword@postgres:5432/vishleshan
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=vishleshan
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgrespassword

REDIS_URL=redis://redis:6379/1
REDIS_HOST=redis
REDIS_PORT=6379
# LOCAL DEVELOPMENT ONLY - Must be replaced with a secure random key in production
JWT_SECRET=dev-secret-jwt-key-replace-in-production-between-gitops
DEBUG=True
ALLOWED_ORIGINS=http://localhost,http://localhost:3000,http://localhost:80,http://127.0.0.1

# AI Keys (LOCAL DEVELOPMENT ONLY: Non-functional placeholder. Replace with real key if testing live LLM)
GEMINI_API_KEY=local-dummy-key-not-a-real-secret
